<nav class="navbar">
    <div class="navbar-container">
        <a href="{{ path('app_landing') }}" class="navbar-logo">
            <img src="{{ asset('images/logo.png') }}" alt="Logo" class="landing-logo">
        </a>
        <ul class="navbar-menu">
            {% if app.user %}
                <li><a href="{{ path('app_dashboard') }}">Dashboard</a></li>
                <li><a href="{{ path('app_team_list') }}">Team</a></li>
                <li><a href="{{ path('app_profile') }}">Profil</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('admin') }}">Admin</a></li>
                {% endif %}
                <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
            {% else %}
                <li><a href="{{ path('app_login') }}">Connexion</a></li>
                <li><a href="{{ path('app_register') }}">Inscription</a></li>
            {% endif %}
        </ul>
    </div>
</nav>